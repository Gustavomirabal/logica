#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <conio.h>

int main(int argc, char const *argv[])
{{
    char Nome[81], mensagem[39], sEsquerd, sDireito, Contorno[42],;
    int Tamanho, lado1=0, me=20, Direito, ns, i, fim, Lado, j;

    printf("\nDestinatario: "); gets(Nome);
    printf("\nMensagem dentro da arvore: "); gets(mensagem);
    Tamanho=strlen(mensagem);
    return 0;
}
